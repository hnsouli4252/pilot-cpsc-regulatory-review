import {readFile,writeFile} from "node:fs/promises";
import {classifyCachedComment,summarizeCachedComment,orderIssueGroups,CATCH_ALL_ISSUES} from "../app/matrix-classification.ts";
const file=new URL("../public/data/cpsc-2025-0009-matrix.json",import.meta.url),snapshot=JSON.parse(await readFile(file,"utf8"));
const baselineRules=[["burden","cost","expense","paperwork","duplicat","economic"],["safety","risk","hazard","injur","death","protect"],["compliance","test","certif","enforce","standard","requirement"],["small business","small entit","manufacturer","retailer","importer","market"],["clarity","guidance","interpret","definition","process","procedure","deadline"],["data","evidence","study","analysis","estimate","research"],["innovation","competition","choice","technology","entrant","design"]];
const before=snapshot.comments.filter((comment:any)=>!baselineRules.some(words=>words.some(word=>comment.excerpt.toLowerCase().includes(word)))).length;
snapshot.comments=snapshot.comments.map((comment:any)=>({...comment,themeTags:classifyCachedComment(comment,"Request for information on reducing regulatory burdens"),summary:summarizeCachedComment(comment)}));
const names=[...new Set<string>(snapshot.comments.flatMap((comment:any)=>comment.themeTags))];snapshot.themes=orderIssueGroups(names.map(theme=>({theme,count:snapshot.comments.filter((comment:any)=>comment.themeTags.includes(theme)).length,commentIds:snapshot.comments.filter((comment:any)=>comment.themeTags.includes(theme)).map((comment:any)=>comment.id),humanReviewStatus:"unreviewed"})));snapshot.unclassifiedTextComments=snapshot.comments.filter((comment:any)=>comment.textAvailable&&comment.themeTags.some((tag:string)=>CATCH_ALL_ISSUES.has(tag))).length;snapshot.unclassifiedComments=snapshot.comments.filter((comment:any)=>comment.themeTags.some((tag:string)=>CATCH_ALL_ISSUES.has(tag))).length;snapshot.regrouping={baselineUnclassifiedComments:before,unclassifiedComments:snapshot.unclassifiedComments,basis:"Cached official comment text plus the selected docket's stated regulatory-burden subject; records without usable official text remain unmatched."};
await writeFile(file,JSON.stringify(snapshot,null,2)+"\n");console.log(JSON.stringify({before,after:snapshot.unclassifiedComments,unmatchedText:snapshot.unclassifiedTextComments,groups:snapshot.themes.map((group:any)=>[group.theme,group.count])},null,2));
